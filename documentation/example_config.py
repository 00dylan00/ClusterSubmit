# Define connection details for SGE and Slurm
sge_config = {
    "host": "your_sge_host",
    "username": "your_username",
    "password": "your_password"  # Use a secure method to provide the password
}

slurm_config = {
    "host": "your_slurm_host",
    "username": "your_username",
    "password": "your_password"  # Use a secure method to provide the password
}
